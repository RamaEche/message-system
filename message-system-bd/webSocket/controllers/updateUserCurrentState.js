const Users = require('../../models/Users.js')

const updateUserCurrentState = async(userId)=>{
/*     const user = await Users.findById(userId)
    user.Chats.Users.forEach(chatId=>{
        
    }) */
}

module.exports = updateUserCurrentState;